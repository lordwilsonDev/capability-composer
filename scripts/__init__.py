"""Repo scripts (live_probe.py).

A regular package (not a namespace dir) so `import scripts.live_probe` in
tests resolves HERE — site-packages ships a same-named `scripts` package that
would otherwise shadow this directory.
"""
